# Evaluation fixtures

버전 관리 가능한 JSON 평가셋만 둔다. 실제 PDF, API 응답, 생성 결과와 document UUID는 저장하지 않는다.

각 파일은 `schema_version`, 문서 식별용 `document`, 하나 이상의 `cases`를 포함한다. 상세 템플릿과 실행 절차는 루트 `README.md`의 **다중 문서 RAG 평가**를 따른다.
